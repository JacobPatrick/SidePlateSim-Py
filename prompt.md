# 平面油膜仿真方法检查

## 项目描述
1. 在给定的平面区域（外啮合圆柱直齿齿轮齿廓）内有油膜，原点处油膜厚度记为 h0
2. 油液的动力粘度记为 μ
3. 油膜夹在两个相对运动的壁面之间，壁面相对绕原点转动，转速记为 n (圈/分钟)
4. 油膜的控制方程为 Renolds 方程：
   \[
   \nabla \cdot (\frac{h^3}{12\mu}\nabla p) = \frac{1}{2} (u, v) \cdot \nabla h + \frac{\partial h}{\partial t}
   \]
   其中 p 为油膜的压强，t 为时间，\nabla = (\frac{\partial}{\partial x}, \frac{\partial}{\partial y})
   注：方程左侧在本项目中使用散度定理转化，最终按网格单元离散、线性化，并重新组装为线性系统以求解
5. 油膜的边界均为开放边界，边界条件为 p = p0，假设所有边界的压强都相等
6. 油膜的厚度随时间变化，变化速度为 \frac{\partial h}{\partial t}
7. 需要建立网格，求解油膜在瞬时的压强分布 p(x, y)
8. 油膜呈楔形，厚度梯度记为 \nabla{h}

## 项目代码
项目完整代码均在根目录下，其中
1. /src/solver/reynolds_solver.py 实现了 Reynolds 
2. /src/scripts/run.py 为入口脚本
3. /config/SimParam_1.yml 为仿真参数配置文件

## 现有问题
本项目仿真结果与商业软件（comsol）在相同参数（详见 /config/SimParam_1.yml）下的仿真结果存在偏差，具体表现为：
1. 压力分布存在较大差异，作用点明显偏离 y 轴，且若手动改变上下壁面的相对运动速度 U 的方向（改为反向），压力作用点偏移至 y 轴的另一侧；而在 comsol 中，压力作用点非常靠近 y 轴，且当相对运动速度方向改变时，压力分布几乎无变化
2. 压力数值存在一定差异，本项目仿真结果压力数值明显小于 comsol 仿真结果

## 你的任务
检查本项目中关于 Reynolds 方程求解的核心代码，结合相关理论，分析可能造成上述两个问题的原因